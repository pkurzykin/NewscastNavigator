# Визуальная доводка рабочих экранов и редактора

Исторический checkpoint. Более поздний прямой запрос пользователя изменил
эталон таблицы на пробу 14 сентября: [текущий результат](UI_SYSTEM_STYLE_CLEANUP_RU.md).

15 сентября 2026. Реализация: `909f367`, ветка `codex/ui-system`,
worktree `/private/tmp/NewscastNavigator-ui-system`.

Пользователь поручил закончить все экраны и принятую таблицу. Основа — макеты
6 сентября плюс принятые панели доступа/шрифта/архива 13 сентября. Проба
14 сентября с оценкой «3 с минусом» не используется. Paper exports неизменны,
новые вызовы Paper не расходованы.

## Что изменено

- Устранён конфликт общих CSS defaults с компонентами: восстановлены компактные
  кнопки, пастельные типы блоков, круглая палитра и размеры полей таблицы.
- У редактора один заголовок карточки; действия процесса находятся в шапке,
  статусы проверки — в компактной строке. Убрана лишняя внешняя карточка.
- Доступ собран в одну полосу. Подпись/цвет отражают фактическую сессию,
  сохранены сообщения другого окна, занятого текста, архива и ошибок.
- Шрифт сценария, добавление блоков, поиск/замена и DOCX находятся в первой
  строке инструментов; форматирование и удаление выбранных — во второй.
- Сохранены синяя шапка `#BEDCE6`, пять столбцов, все типы/поля/файлы/TC,
  перетаскивание, ширины, CaptionPanels, undo/redo, autosave и настройки шрифтов.
  Иконки B/I/S и undo/redo сохраняют доступные имена и обработчики.
- Список, производство, архив, история, сотрудники, уведомления и диалоги
  приведены к общей плотности. Фильтры/очередь внимания компактнее,
  технические подписи удалены. Архивное подтверждение поясняет сохранение
  внешних файлов материалов.

## Проверки

- RED: два заголовка сценария вместо одного; после исправления GREEN.
- Полный frontend: **558 passed / 59 files**, без unhandled errors.
- TypeScript + Vite build: **PASS**.
- Полный browser: **149 passed, 2 прежних BFCache skips**. Экраны 1366/1920,
  общая геометрия также 2560. Проверены ввод/выделение, типографика/шрифты,
  поиск, перетаскивание, доступ, autosave, workflow, archive/history.
- После финальной CSS-поправки и добавления проверки переноса действий:
  **8/8 editor browser checks** на 1366/1920. Pending и ошибка остаются в шапке.
- Новая визуальная регрессия проверяет режим, один заголовок, синюю шапку
  выше 500 px, компактные кнопки/круглую палитру и текст в первом viewport.
  На просмотренном снимке 1366 синяя шапка начинается около 425 px.
- Несценарный пакет дополнительно: 112 component и 6 browser checks.
- Независимое ревью Codex: функциональных блокеров нет. Замечание о рамке
  access-bar при промежуточном уведомлении исправлено: склейка с toolbar
  применяется только при непосредственном соседстве.
- `git diff --check`: PASS. Логи и снимки: `output/visual-polish/`.

Backend не менялся. Предыдущий полный результат 1072 passed / 4 PostgreSQL-only
skips и настоящий PostgreSQL smoke описаны в `UI_SYSTEM_LIVE_CHECK_RU.md`;
повторным тестом этого checkpoint они не называются.

## Локальная приёмка

Стенд `ncn-ui-system-preview` пересобран из этих исходников; frontend/backend/DB
healthy. Повторно проверены реальные список, производство и сценарий 36 через
API. Основной шрифт Franklin Gothic Book и четыре строки сохранены.
Редактор открыт: `http://127.0.0.1:5173/stories/36/scenario`.

Реализация принятого дизайна завершена для локальной приёмки. Собственная
дизайн-система используется; финальная визуальная оценка остаётся за Павлом.
Прежние платформенные проверки физических клавиатур, Word/Windows и BFCache
сохраняются в handoff; они не означают отложенную перерисовку таблицы.
Основной checkout и внешние серверы не затронуты. Push/PR/merge/deploy
не выполнялись. CodeRabbit не использовался.
