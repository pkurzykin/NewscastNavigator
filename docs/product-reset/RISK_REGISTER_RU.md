# NewscastNavigator Product Reset — реестр рисков

| ID | Риск | Текущее состояние | Обнаружение | Снижение |
|---|---|---|---|---|
| R01 | Исчезновение текста и сдвиг страницы при autosave | Снижен CP7 binding: local-authoritative single-flight, server-expiry recovery через новую lease, adverse-order reload и fixed-layout status прошли component/browser boundary | полный CP7 frontend/browser runner на `1366`/`1920`; actual BFCache недоступен в headless и покрыт deterministic lifecycle matrix | повторить actual BFCache только на отдельно разрешённой внешней демонстрации |
| R02 | Нарушение CaptionPanels mapping | Снижен EXT.2: import на exact deployed SHA совпал с текущей revision `1` и тремя сегментами, exact opened marker обновился; сюжет штатно возвращён в архив | CP1 characterization, CP3 current/latest scenario tests, CP7 runner-owned backend full suite и разрешённый внешний walkthrough | сохранять current/latest marker contract |
| R03 | Тесты затронут не ту PostgreSQL | Снижен CP7 binding: rehearsal разрешает только `nn-product-reset-eval-*`, restore проверяет пустую БД до импорта, source/restore projects удаляются через `down -v` без оставшихся resources | isolated project guard, empty-database restore test и bound run `20260725T090315Z-c4a097eb5cee-adfb9934` с отдельными source/restore volumes | destructive steps оставлять только в isolated eval environment |
| R04 | Auth/bootstrap регрессия при чистой схеме | Снижен EXT.2: чистая PostgreSQL, import, explicit bootstrap, authenticated smoke и default credential rejection прошли на exact deployed SHA | CP2 auth/bootstrap/migration tests и разрешённый внешний smoke | PBKDF2, explicit bootstrap, без legacy fallback |
| R05 | Ложный зелёный eval | Снижен EXT.2: immutable CP7 binding дополнен redacted external evidence exact deployed SHA, backup, dataset validation, auth, двумя viewports и CaptionPanels; artifact hashes проверяются против Git blobs | backend `826`, frontend `118`, build `155`, browser `25+25`, compile/pip/license/Compose/rehearsal и external demo checks — exit `0`; negative binding/drift/artifact tests | сохранять immutable binding и не коммитить dataset/screenshots |
| R06 | Устаревший deploy/backup/restore путь останется активным | Снижен CP7: оставлены один local, один test и один demo Compose path; restore использует distinct lowercase eval project, exact compose/env, empty DB и `down -v` trap; full counts/auth smoke закреплены за canonical rehearsal | operations/current-doc contracts, три Compose config gate и bound clean rehearsal `20260725T090315Z-c4a097eb5cee-adfb9934` с atomic pointer | сохранять один канонический rehearsal/restore path |
| R07 | Реальные данные попадут в seed/evidence | Снижен EXT.2: automated seed/rehearsal используют только synthetic records; внешний dataset прошёл fail-closed validator, хранится отдельно mode `600`, а tracked evidence содержит только redacted IDs и SHA-256 | `test_demo_dataset_validation.py`, focused `124 passed`, expanded boundary `324 passed`, manual review `0/0/0`, reusable synthetic policy, staged-source scanner, external validation и Git-blob artifact guard | внешний dataset и screenshots не добавлять в Git; structural FQDN check не считать DNS/ownership validation |
| R08 | Legacy останется в runtime или текущей документации после перехода | Снижен CP7 binding: CP2 bridge/services отсутствуют, old editor GET/PUT отвечают `404`, 29 противоречащих legacy/contracts/archive/design docs удалены | denylist, repository policy, current-doc policy, legacy 404 tests в полном CP7 runner | сохранять denylist и current-doc policy |
| R09 | UX оценят по build, без фактического интерфейса | Снижен EXT.2: помимо CP7 browser `25+25`, реальный deployed UI проверен в Chromium на `1366x768` и `1920x1080`, без horizontal overflow и console errors в чистой auth-вкладке | accessibility/axe evidence, визуальная матрица, score `90/100`, external screenshot hashes | сохранять immutable evidence hashes; actual BFCache остаётся отдельным необязательным полевым наблюдением |
| R10 | Базовые SHA смешаются | Снижен | eval schema и CP1 evidence содержат две базы и exact checkpoint SHAs | два обязательных именованных поля с точными значениями |
| R11 | Формальный UX score станет самооценкой без воспроизводимых доказательств | Снижен CP7 binding: strict validator связывает `90/100`, rationale, полный screenshot/axe matrix, фиксированные paths и SHA-256 с exact runner evidence | backend negative matrix, actual-artifact boundary, полный browser `25+25`, все 12 artifacts проходят local hash validation и immutable subtree pin | повторить видимый walkthrough только на отдельно разрешённом external demo |
| R12 | Уязвимые или нелицензированные зависимости попадут в runtime | Частично снижен CP7: hash locks связаны с direct specifiers/pyproject и имеют одинаковый runtime graph; Linux `aarch64`/`x86_64` SQLAlchemy marker graph включает exact `greenlet`; checker fail-closed покрывает 33 Python runtime/direct-dev packages, direct npm lock metadata и exact Onest/OFL hashes/notices; npm audit сохраняет 2 transitive runtime findings через не импортируемый markdown subtree и 7 dev-tool findings | clean Python 3.11 lock install, Linux/aarch64 Docker build, double byte-identical lock regeneration, compile, `pip check`, metadata/license checker, clean `npm ci` и полный CP7 runner | не применять blind force-upgrade; выделить test-first совместимое обновление TipTap/Vite/Vitest, production держит только static build |
| R13 | Разрешённые demo-сюжеты недостаточно содержательны | Открыт как внешний data-quality риск: из двух архивных source-сюжетов один имеет пустые строки, второй — только короткие служебные строки; данные не дополнялись синтетикой и не смешивались с неподтверждёнными рабочими сюжетами | агрегатная проверка legacy source и визуальный walkthrough обоих архивных сценариев | перед показом руководству отдельно подготовить разрешённый содержательный набор, повторив тот же sanitizer/validator/evidence flow |

## Текущая граница

CP1–CP7 evidence привязаны к exact historical subtrees. CP7 source `c4a097eb5cee226c884adadf0ac79958b8a71e53` подтверждён полным runner и evidence commit `2194f5986146c3677bc7da794683bf00d164ae30`: backend `826`, frontend `118`, build `155`, browser `25+25`, compile/pip/license/Compose/rehearsal — все exit `0`; UX `90/100`, operations findings пусты. EXT.2 добавил redacted external evidence exact deploy, backup, validated dataset, auth, двух desktop viewports и CaptionPanels current revision; dataset/screenshots остались untracked. `local_hard_gates_passed=true`, `hard_gates_passed=true`, `full_eval_passed=true`.
