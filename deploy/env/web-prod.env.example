ENVIRONMENT=production
POSTGRES_DB=newscast
POSTGRES_USER=newscast
POSTGRES_PASSWORD=change-this-db-password
DATABASE_URL=postgresql+psycopg://newscast:change-this-db-password@db:5432/newscast
SECRET_KEY=change-this-session-secret
SEED_DEMO_DATA=false
# Для production обязательно замени placeholder-значения выше на реальные.
# Runtime откажется стартовать с demo-seed или change-this-* секретами/паролями.
STORAGE_PATH=/app/storage
EXPORT_PATH=/app/exports

# Публичный bind теперь отдается edge reverse proxy.
# Для безопасного bootstrap нового сервера оставляй loopback, а публичный доступ включай
# только после аудита и явного решения.
NGINX_BIND_HOST=127.0.0.1
NGINX_HTTP_PORT=8088
NGINX_SERVER_NAME=ncastnav.ru www.ncastnav.ru

# Для production за reverse proxy укажи реальный внешний origin.
# Для CEP/CaptionPanels one-click fetch также нужен Origin: null.
CORS_ORIGINS=http://ncastnav.ru,http://www.ncastnav.ru,null

# Если frontend и backend живут за одним reverse proxy, оставляем пустым.
FRONTEND_VITE_API_BASE_URL=

MAX_UPLOAD_SIZE_MB=512
ALLOWED_UPLOAD_EXTENSIONS=.mp4,.mov,.mxf,.mp3,.wav,.m4a,.aac,.jpg,.jpeg,.png,.webp,.pdf,.docx,.txt
