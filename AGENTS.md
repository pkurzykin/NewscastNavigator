# AGENTS.md — NewscastNavigator

## Язык

Общайся с пользователем на русском языке. В коде используй принятый в проекте язык имён и интерфейсов; пользовательские тексты должны быть на русском.

## Source of truth

Перед заметной работой прочитай:

1. `docs/product-reset/SPEC_RU.md` — утверждённая продуктовая модель.
2. `docs/product-reset/EVAL_RUBRIC_RU.md` — определение готовности.
3. `docs/product-reset/IMPLEMENTATION_PLAN_RU.md`, если он уже утверждён.
4. `docs/product-reset/GOAL_PROMPTS_RU.md` — порядок Plan/Goal.

При противоречии эти документы имеют приоритет над старой документацией и текущей реализацией. Старые документы до ревью являются историческим контекстом, а не запретом на изменение.

## Product Reset contract

- Текущий проект — прототип без ценных рабочих данных.
- Обратная совместимость и миграция старых данных не требуются.
- Разрешено удалять и радикально менять код, API, таблицы, миграции, тесты, документы и скрипты в пределах утверждённого плана.
- Сохраняй рабочее пользовательское поведение табличного редактора сценария и CaptionPanels; сначала закрепляй его characterization-тестами.
- В продукте существует один актуальный сценарий. Любая разрешённая сохранённая правка сразу актуальна.
- Проверка и корректура — workflow-отметки, а не отдельные копии текста.
- Поздняя мелкая правка не снимает корректуру автоматически; решение о повторной вычитке принимает начальник или шеф.
- Не возвращай ручные версии, branch/merge, выбор текущего текста, отдельные монтажные/титровые копии и общую ленту комментариев.
- Не создавай параллельный пользовательский режим v2.
- Не добавляй функции из раздела «Явные нецели» спецификации.
- Git history — архив удалённого legacy; не создавай папку `legacy`.

## Порядок работы

- До реализации выполни read-only `/plan` и получи явное утверждение file-level плана.
- Работай в отдельной ветке или worktree; не меняй `main` напрямую.
- Сначала воспроизводи поведение или баг тестом, затем меняй код.
- Делай вертикальные checkpoints, каждый из которых можно проверить независимо.
- Удаляй заменённый legacy в том же checkpoint.
- После каждого checkpoint запускай релевантные тесты, полный доступный набор и browser-проверку.
- Проверяй фактический интерфейс, а не только build и логи.
- Веди `docs/product-reset/PROGRESS.md` во время реализации.
- Создавай небольшие осмысленные локальные коммиты.
- Не push, не открывай PR, не merge и не deploy без отдельной команды пользователя.
- Если решение меняет утверждённый продукт, остановись и задай один конкретный вопрос.

## Автосохранение

- Локальное состояние редактора является главным во время ввода.
- Ответ сервера не должен целиком заменять открытый редактор.
- Устаревший ответ не может перезаписать свежий ввод.
- Стабильные идентификаторы строк создаются до первого сохранения.
- Успешное сохранение работает тихо и не меняет layout, focus, selection или scroll.
- Ошибка не должна скрывать или уничтожать локальный текст.

## Проверки

Текущие базовые команды до file-level ревью:

```bash
cd backend && pytest -q
cd frontend && npm ci && npm run build
docker compose -f compose.yaml config
```

Frontend component- и browser-тесты должны быть добавлены планом Product Reset. Финальные критерии всегда берутся из `docs/product-reset/EVAL_RUBRIC_RU.md`.

## Эксплуатационный код

- Проведи два прохода: ранний inventory и финальный clean-deploy rehearsal.
- Для каждого deploy-, migration-, backup-, restore-, CI-, seed-, smoke- и recovery-файла зафиксируй `KEEP`, `ADAPT`, `REPLACE` или `DELETE`.
- Оставь по одному каноническому пути локальной разработки и демонстрационного deploy.
- Проверь чистую PostgreSQL, миграцию, synthetic seed, health, smoke, backup и restore.
- Не читай, не печатай и не коммить реальные секреты или production `.env`.
- Не выполняй действия на домашнем или будущем рабочем сервере без явного разрешения.

## Данные и демонстрация

- Автоматические seed и тесты используют только синтетические данные.
- Не коммить реальные сюжеты, фамилии коллег, контакты или пути к материалам.
- Внутренний демонстрационный набор с завершёнными сюжетами готовится отдельно и не становится тестовой фикстурой репозитория.

## Документация и отчёт

- Изменяй документацию вместе с поведением.
- Не сохраняй противоречащий документ «на всякий случай»; перепиши или удали его в соответствующем checkpoint.
- После блока сообщай: что изменено, чем проверено, что удалено и какие риски остались.
- Авторская политика проекта: `© 2026 Павел Курзыкин. Все права защищены.` Codex не должен самостоятельно составлять или подписывать юридический договор от имени Павла или компании.
