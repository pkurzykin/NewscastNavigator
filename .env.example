ENVIRONMENT=production
POSTGRES_DB=newscast
POSTGRES_USER=newscast
POSTGRES_PASSWORD=change-this-db-password
DATABASE_URL=postgresql+psycopg://newscast:change-this-db-password@db:5432/newscast
SECRET_KEY=change-this-session-secret
SEED_DEMO_DATA=false
# Для production обязательно замени placeholder-значения выше на реальные.
# Runtime теперь откажется стартовать в ENVIRONMENT=production с demo-seed или change-this-* секретами.
# По умолчанию production edge слушает только loopback.
# Публичный bind 0.0.0.0 включай только после DNS/TLS/access policy.
NGINX_BIND_HOST=127.0.0.1
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
NGINX_SERVER_NAME=example.com www.example.com
SSL_CERT_PATH=/etc/newscast-web/ssl/example.com/fullchain.pem
SSL_KEY_PATH=/etc/newscast-web/ssl/example.com/privkey.pem

# Для web UI и CEP/CaptionPanels one-click fetch.
# CEP-панель может отправлять Origin: null, поэтому null должен быть разрешен явно.
# Для production замени example.com на утвержденный домен.
CORS_ORIGINS=https://example.com,https://www.example.com,null
STORAGE_PATH=/app/storage
EXPORT_PATH=/app/exports
ALLOWED_UPLOAD_EXTENSIONS=.mp4,.mov,.mxf,.mp3,.wav,.m4a,.aac,.jpg,.jpeg,.png,.webp,.pdf,.docx,.txt
MAX_UPLOAD_SIZE_MB=512
FRONTEND_VITE_API_BASE_URL=
